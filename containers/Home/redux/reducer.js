export const initialState = {
  firstName: "",
  lastName: "",
};

export const reducerHome = (state = initialState, action) => {
  switch (action?.type) {
    default:
      return state;
  }
};
